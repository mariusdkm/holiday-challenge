services:
  db:
    image: postgres
    restart: always
    ports:
      - "5432:5432"
    env_file:
      .env
    volumes:
      - ./postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready" ]
      interval: 1s
      timeout: 5s
      retries: 10